# 视频去水印助手_打包版

双击 `启动视频去水印助手.bat` 即可启动。

如果当前文件夹里没有 `_python_deps`，脚本会自动把 `imageio-ffmpeg` 安装到这个目录里。这个依赖目录只给本工具使用，不会污染系统 Python 环境。

注意：

- 电脑需要先安装 Python 3.10 或更新版本，并且 `python` 命令能在命令行里运行。
- 第一次安装依赖需要联网。
- 整个文件夹可以复制到别的位置使用。
- 仅用于处理你本人拥有版权或已获授权的视频。
